import wollok.game.*
import plantas.*
import granjeros.*

program farmVille {
//	CONFIG	
	game.title("FARMVILLE DE TAVO")
	game.height(10)
	game.width(18)
	game.ground("ground.png")
//	VISUALES
	game.addVisual(hector)
	keyboard.up().onPressDo{ hector.moverHaciaArriba() }
	keyboard.down().onPressDo{ hector.moverHaciaAbajo()}
	keyboard.left().onPressDo{ hector.moverHaciaIzq() }
	keyboard.right().onPressDo{ hector.moverHaciaDer() }
	game.errorReporter(hector)
	keyboard.m().onPressDo{ hector.plantarMaiz() }
	keyboard.t().onPressDo{ hector.plantarTrigo() }
	keyboard.o().onPressDo{ hector.plantarTomaco() }
	keyboard.r().onPressDo{ hector.regar() }
	game.start()
}
